<!-- Implement a full authenciation flow, using cookies for refresh tokens, ID token, to extract the user's information, and access tokens to gain access to the api based on the role and privileges the user has. Thee ID token and the access token will be stored in memory, while the refresh token will be via a cookie from the server side, also implement the remember me functionality , using the server to verify a token, and return true, so the user can just enter only the password need, no need for email, then the sso authenication using google account, facebook, and other social media account. -->